data:
  class_path: mst.dataloader.PairedMixDataModule
  init_args:
    data_dir: /content/musdb18
    metadata_file: ./data/musdb18-aug.yaml
    length: 882000   # 20 seconds at 44.1 kHz
    # min_tracks: 2
    # max_tracks: 4
    batch_size: 2
    num_workers: 4
    # num_train_passes: 20
    # num_val_passes: 4
    # train_buffer_size_gb: 1.0
    # val_buffer_size_gb: 0.25
    # target_track_lufs_db: -48.0